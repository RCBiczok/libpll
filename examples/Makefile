# Makefile.in generated by automake 1.13.4 from Makefile.am.
# examples/Makefile.  Generated from Makefile.in by configure.

# Copyright (C) 1994-2013 Free Software Foundation, Inc.

# This Makefile.in is free software; the Free Software Foundation
# gives unlimited permission to copy and/or distribute it,
# with or without modifications, as long as this notice is preserved.

# This program is distributed in the hope that it will be useful,
# but WITHOUT ANY WARRANTY, to the extent permitted by law; without
# even the implied warranty of MERCHANTABILITY or FITNESS FOR A
# PARTICULAR PURPOSE.




am__is_gnu_make = test -n '$(MAKEFILE_LIST)' && test -n '$(MAKELEVEL)'
am__make_running_with_option = \
  case $${target_option-} in \
      ?) ;; \
      *) echo "am__make_running_with_option: internal error: invalid" \
              "target option '$${target_option-}' specified" >&2; \
         exit 1;; \
  esac; \
  has_opt=no; \
  sane_makeflags=$$MAKEFLAGS; \
  if $(am__is_gnu_make); then \
    sane_makeflags=$$MFLAGS; \
  else \
    case $$MAKEFLAGS in \
      *\\[\ \	]*) \
        bs=\\; \
        sane_makeflags=`printf '%s\n' "$$MAKEFLAGS" \
          | sed "s/$$bs$$bs[$$bs $$bs	]*//g"`;; \
    esac; \
  fi; \
  skip_next=no; \
  strip_trailopt () \
  { \
    flg=`printf '%s\n' "$$flg" | sed "s/$$1.*$$//"`; \
  }; \
  for flg in $$sane_makeflags; do \
    test $$skip_next = yes && { skip_next=no; continue; }; \
    case $$flg in \
      *=*|--*) continue;; \
        -*I) strip_trailopt 'I'; skip_next=yes;; \
      -*I?*) strip_trailopt 'I';; \
        -*O) strip_trailopt 'O'; skip_next=yes;; \
      -*O?*) strip_trailopt 'O';; \
        -*l) strip_trailopt 'l'; skip_next=yes;; \
      -*l?*) strip_trailopt 'l';; \
      -[dEDm]) skip_next=yes;; \
      -[JT]) skip_next=yes;; \
    esac; \
    case $$flg in \
      *$$target_option*) has_opt=yes; break;; \
    esac; \
  done; \
  test $$has_opt = yes
am__make_dryrun = (target_option=n; $(am__make_running_with_option))
am__make_keepgoing = (target_option=k; $(am__make_running_with_option))
pkgdatadir = $(datadir)/libpll
pkgincludedir = $(includedir)/libpll
pkglibdir = $(libdir)/libpll
pkglibexecdir = $(libexecdir)/libpll
am__cd = CDPATH="$${ZSH_VERSION+.}$(PATH_SEPARATOR)" && cd
install_sh_DATA = $(install_sh) -c -m 644
install_sh_PROGRAM = $(install_sh) -c
install_sh_SCRIPT = $(install_sh) -c
INSTALL_HEADER = $(INSTALL_DATA)
transform = $(program_transform_name)
NORMAL_INSTALL = :
PRE_INSTALL = :
POST_INSTALL = :
NORMAL_UNINSTALL = :
PRE_UNINSTALL = :
POST_UNINSTALL = :
build_triplet = x86_64-suse-linux-gnu
host_triplet = x86_64-suse-linux-gnu
DIST_COMMON = $(srcdir)/heterotachy/target.mk $(srcdir)/lg4/target.mk \
	$(srcdir)/load-utree/target.mk \
	$(srcdir)/newick-fasta-rooted/target.mk \
	$(srcdir)/newick-fasta-unrooted/target.mk \
	$(srcdir)/newick-phylip-unrooted/target.mk \
	$(srcdir)/newton/target.mk $(srcdir)/parsimony/target.mk \
	$(srcdir)/partial-traversal/target.mk \
	$(srcdir)/protein-list/target.mk $(srcdir)/rooted/target.mk \
	$(srcdir)/rooted-tacg/target.mk $(srcdir)/unrooted/target.mk \
	$(srcdir)/stepwise/target.mk $(srcdir)/Makefile.in \
	$(srcdir)/Makefile.am $(dist_heterotachy_DATA) \
	$(dist_lg4_DATA) $(dist_lg4_data_DATA) $(dist_load_utree_DATA) \
	$(dist_newick_fasta_rooted_DATA) \
	$(dist_newick_fasta_unrooted_DATA) \
	$(dist_newick_phylip_unrooted_DATA) $(dist_newton_DATA) \
	$(dist_parsimony_DATA) $(dist_partial_traversal_DATA) \
	$(dist_protein_list_DATA) $(dist_rooted_DATA) \
	$(dist_rooted_tacg_DATA) $(dist_stepwise_DATA) \
	$(dist_unrooted_DATA)
subdir = examples
ACLOCAL_M4 = $(top_srcdir)/aclocal.m4
am__aclocal_m4_deps = $(top_srcdir)/m4/libtool.m4 \
	$(top_srcdir)/m4/ltoptions.m4 $(top_srcdir)/m4/ltsugar.m4 \
	$(top_srcdir)/m4/ltversion.m4 $(top_srcdir)/m4/lt~obsolete.m4 \
	$(top_srcdir)/configure.ac
am__configure_deps = $(am__aclocal_m4_deps) $(CONFIGURE_DEPENDENCIES) \
	$(ACLOCAL_M4)
mkinstalldirs = $(install_sh) -d
CONFIG_HEADER = $(top_builddir)/config.h
CONFIG_CLEAN_FILES =
CONFIG_CLEAN_VPATH_FILES =
AM_V_P = $(am__v_P_$(V))
am__v_P_ = $(am__v_P_$(AM_DEFAULT_VERBOSITY))
am__v_P_0 = false
am__v_P_1 = :
AM_V_GEN = $(am__v_GEN_$(V))
am__v_GEN_ = $(am__v_GEN_$(AM_DEFAULT_VERBOSITY))
am__v_GEN_0 = @echo "  GEN     " $@;
am__v_GEN_1 = 
AM_V_at = $(am__v_at_$(V))
am__v_at_ = $(am__v_at_$(AM_DEFAULT_VERBOSITY))
am__v_at_0 = @
am__v_at_1 = 
SOURCES =
DIST_SOURCES =
am__can_run_installinfo = \
  case $$AM_UPDATE_INFO_DIR in \
    n|no|NO) false;; \
    *) (install-info --version) >/dev/null 2>&1;; \
  esac
am__vpath_adj_setup = srcdirstrip=`echo "$(srcdir)" | sed 's|.|.|g'`;
am__vpath_adj = case $$p in \
    $(srcdir)/*) f=`echo "$$p" | sed "s|^$$srcdirstrip/||"`;; \
    *) f=$$p;; \
  esac;
am__strip_dir = f=`echo $$p | sed -e 's|^.*/||'`;
am__install_max = 40
am__nobase_strip_setup = \
  srcdirstrip=`echo "$(srcdir)" | sed 's/[].[^$$\\*|]/\\\\&/g'`
am__nobase_strip = \
  for p in $$list; do echo "$$p"; done | sed -e "s|$$srcdirstrip/||"
am__nobase_list = $(am__nobase_strip_setup); \
  for p in $$list; do echo "$$p $$p"; done | \
  sed "s| $$srcdirstrip/| |;"' / .*\//!s/ .*/ ./; s,\( .*\)/[^/]*$$,\1,' | \
  $(AWK) 'BEGIN { files["."] = "" } { files[$$2] = files[$$2] " " $$1; \
    if (++n[$$2] == $(am__install_max)) \
      { print $$2, files[$$2]; n[$$2] = 0; files[$$2] = "" } } \
    END { for (dir in files) print dir, files[dir] }'
am__base_list = \
  sed '$$!N;$$!N;$$!N;$$!N;$$!N;$$!N;$$!N;s/\n/ /g' | \
  sed '$$!N;$$!N;$$!N;$$!N;s/\n/ /g'
am__uninstall_files_from_dir = { \
  test -z "$$files" \
    || { test ! -d "$$dir" && test ! -f "$$dir" && test ! -r "$$dir"; } \
    || { echo " ( cd '$$dir' && rm -f" $$files ")"; \
         $(am__cd) "$$dir" && rm -f $$files; }; \
  }
am__installdirs = "$(DESTDIR)$(heterotachydir)" "$(DESTDIR)$(lg4dir)" \
	"$(DESTDIR)$(lg4_datadir)" "$(DESTDIR)$(load_utreedir)" \
	"$(DESTDIR)$(newick_fasta_rooteddir)" \
	"$(DESTDIR)$(newick_fasta_unrooteddir)" \
	"$(DESTDIR)$(newick_phylip_unrooteddir)" \
	"$(DESTDIR)$(newtondir)" "$(DESTDIR)$(parsimonydir)" \
	"$(DESTDIR)$(partial_traversaldir)" \
	"$(DESTDIR)$(protein_listdir)" "$(DESTDIR)$(rooteddir)" \
	"$(DESTDIR)$(rooted_tacgdir)" "$(DESTDIR)$(stepwisedir)" \
	"$(DESTDIR)$(unrooteddir)"
DATA = $(dist_heterotachy_DATA) $(dist_lg4_DATA) $(dist_lg4_data_DATA) \
	$(dist_load_utree_DATA) $(dist_newick_fasta_rooted_DATA) \
	$(dist_newick_fasta_unrooted_DATA) \
	$(dist_newick_phylip_unrooted_DATA) $(dist_newton_DATA) \
	$(dist_parsimony_DATA) $(dist_partial_traversal_DATA) \
	$(dist_protein_list_DATA) $(dist_rooted_DATA) \
	$(dist_rooted_tacg_DATA) $(dist_stepwise_DATA) \
	$(dist_unrooted_DATA)
am__tagged_files = $(HEADERS) $(SOURCES) $(TAGS_FILES) $(LISP)
DISTFILES = $(DIST_COMMON) $(DIST_SOURCES) $(TEXINFOS) $(EXTRA_DIST)
ACLOCAL = ${SHELL} /home/hpc/pr63lo/di56sad2/rudolf/libpll/missing aclocal-1.13
ALLOCA = 
AMTAR = $${TAR-tar}
AM_AVX2_FLAGS = -march=core-avx2
AM_AVX512F_FLAGS = -mavx512f
AM_DEFAULT_VERBOSITY = 1
AR = ar
AUTOCONF = ${SHELL} /home/hpc/pr63lo/di56sad2/rudolf/libpll/missing autoconf
AUTOHEADER = ${SHELL} /home/hpc/pr63lo/di56sad2/rudolf/libpll/missing autoheader
AUTOMAKE = ${SHELL} /home/hpc/pr63lo/di56sad2/rudolf/libpll/missing automake-1.13
AWK = gawk
CC = gcc
CCDEPMODE = depmode=gcc3
CFLAGS = -g
CPP = gcc -E
CPPFLAGS = 
CYGPATH_W = echo
DEFS = -DHAVE_CONFIG_H
DEPDIR = .deps
DLLTOOL = false
DSYMUTIL = 
DUMPBIN = 
ECHO_C = 
ECHO_N = -n
ECHO_T = 
EGREP = /usr/bin/grep -E
EXEEXT = 
FGREP = /usr/bin/grep -F
GREP = /usr/bin/grep
HAVE_PS2PDF = yes
INSTALL = /usr/bin/install -c
INSTALL_DATA = ${INSTALL} -m 644
INSTALL_PROGRAM = ${INSTALL}
INSTALL_SCRIPT = ${INSTALL}
INSTALL_STRIP_PROGRAM = $(install_sh) -c -s
LD = /usr/bin/ld -m elf_x86_64
LDFLAGS = 
LEX = flex
LEXLIB = -lfl
LEX_OUTPUT_ROOT = lex.yy
LIBOBJS = 
LIBS = -lm 
LIBTOOL = $(SHELL) $(top_builddir)/libtool
LIPO = 
LN_S = ln -s
LTLIBOBJS = 
MAKEINFO = ${SHELL} /home/hpc/pr63lo/di56sad2/rudolf/libpll/missing makeinfo
MANIFEST_TOOL = :
MKDIR_P = /usr/bin/mkdir -p
NM = /usr/bin/nm -B
NMEDIT = 
OBJDUMP = objdump
OBJEXT = o
OTOOL = 
OTOOL64 = 
PACKAGE = libpll
PACKAGE_BUGREPORT = Tomas.Flouri@h-its.org
PACKAGE_NAME = libpll
PACKAGE_STRING = libpll 0.3.2
PACKAGE_TARNAME = libpll
PACKAGE_URL = 
PACKAGE_VERSION = 0.3.2
PATH_SEPARATOR = :
RANLIB = ranlib
SED = /usr/bin/sed
SET_MAKE = 
SHELL = /bin/sh
STRIP = strip
VERSION = 0.3.2
YACC = bison -y
YFLAGS = 
abs_builddir = /home/hpc/pr63lo/di56sad2/rudolf/libpll/examples
abs_srcdir = /home/hpc/pr63lo/di56sad2/rudolf/libpll/examples
abs_top_builddir = /home/hpc/pr63lo/di56sad2/rudolf/libpll
abs_top_srcdir = /home/hpc/pr63lo/di56sad2/rudolf/libpll
ac_ct_AR = ar
ac_ct_CC = gcc
ac_ct_DUMPBIN = 
am__include = include
am__leading_dot = .
am__quote = 
am__tar = $${TAR-tar} chof - "$$tardir"
am__untar = $${TAR-tar} xf -
bindir = ${exec_prefix}/bin
build = x86_64-suse-linux-gnu
build_alias = 
build_cpu = x86_64
build_os = linux-gnu
build_vendor = suse
builddir = .
datadir = ${datarootdir}
datarootdir = ${prefix}/share
docdir = ${datarootdir}/doc/${PACKAGE_TARNAME}
dvidir = ${docdir}
exec_prefix = ${prefix}
host = x86_64-suse-linux-gnu
host_alias = 
host_cpu = x86_64
host_os = linux-gnu
host_vendor = suse
htmldir = ${docdir}
includedir = ${prefix}/include
infodir = ${datarootdir}/info
install_sh = ${SHELL} /home/hpc/pr63lo/di56sad2/rudolf/libpll/install-sh
libdir = ${exec_prefix}/lib
libexecdir = ${exec_prefix}/libexec
localedir = ${datarootdir}/locale
localstatedir = ${prefix}/var
mandir = ${datarootdir}/man
mkdir_p = $(MKDIR_P)
oldincludedir = /usr/include
pdfdir = ${docdir}
prefix = /usr/local
program_transform_name = s,x,x,
psdir = ${docdir}
sbindir = ${exec_prefix}/sbin
sharedstatedir = ${prefix}/com
srcdir = .
sysconfdir = ${prefix}/etc
target_alias = 
top_build_prefix = ../
top_builddir = ..
top_srcdir = ..
heterotachydir = $(docdir)/examples/heterotachy
dist_heterotachy_DATA = heterotachy/heterotachy.c heterotachy/Makefile heterotachy/README.md
lg4dir = $(docdir)/examples/lg4
lg4_datadir = $(docdir)/examples/lg4/data
dist_lg4_DATA = lg4/lg4.c lg4/Makefile
dist_lg4_data_DATA = lg4/data/example.fas lg4/data/example.tree
load_utreedir = $(docdir)/examples/load-utree
dist_load_utree_DATA = load-utree/load-utree.c load-utree/Makefile load-utree/README.md
newick_fasta_rooteddir = $(docdir)/examples/newick-fasta-rooted
dist_newick_fasta_rooted_DATA = newick-fasta-rooted/newick-fasta-rooted.c newick-fasta-rooted/Makefile
newick_fasta_unrooteddir = $(docdir)/examples/newick-fasta-unrooted
dist_newick_fasta_unrooted_DATA = newick-fasta-unrooted/newick-fasta-unrooted.c newick-fasta-unrooted/Makefile
newick_phylip_unrooteddir = $(docdir)/examples/newick-phylip-unrooted
dist_newick_phylip_unrooted_DATA = newick-phylip-unrooted/newick-phylip-unrooted.c newick-phylip-unrooted/Makefile
newtondir = $(docdir)/examples/newton
dist_newton_DATA = newton/newton.c newton/Makefile newton/README.md
parsimonydir = $(docdir)/examples/parsimony
dist_parsimony_DATA = parsimony/npr-pars.c parsimony/Makefile
partial_traversaldir = $(docdir)/examples/partial-traversal
dist_partial_traversal_DATA = partial-traversal/partial.c partial-traversal/Makefile
protein_listdir = $(docdir)/examples/protein-list
dist_protein_list_DATA = protein-list/protein-list.c protein-list/Makefile
rooteddir = $(docdir)/examples/rooted
dist_rooted_DATA = rooted/rooted.c rooted/Makefile rooted/README.md
rooted_tacgdir = $(docdir)/examples/rooted-tacg
dist_rooted_tacg_DATA = rooted-tacg/rooted-tacg.c rooted-tacg/Makefile
unrooteddir = $(docdir)/examples/unrooted
dist_unrooted_DATA = unrooted/unrooted.c unrooted/Makefile unrooted/README.md
stepwisedir = $(docdir)/examples/stepwise
dist_stepwise_DATA = stepwise/stepwise.c stepwise/Makefile
all: all-am

.SUFFIXES:
$(srcdir)/Makefile.in:  $(srcdir)/Makefile.am $(srcdir)/heterotachy/target.mk $(srcdir)/lg4/target.mk $(srcdir)/load-utree/target.mk $(srcdir)/newick-fasta-rooted/target.mk $(srcdir)/newick-fasta-unrooted/target.mk $(srcdir)/newick-phylip-unrooted/target.mk $(srcdir)/newton/target.mk $(srcdir)/parsimony/target.mk $(srcdir)/partial-traversal/target.mk $(srcdir)/protein-list/target.mk $(srcdir)/rooted/target.mk $(srcdir)/rooted-tacg/target.mk $(srcdir)/unrooted/target.mk $(srcdir)/stepwise/target.mk $(am__configure_deps)
	@for dep in $?; do \
	  case '$(am__configure_deps)' in \
	    *$$dep*) \
	      ( cd $(top_builddir) && $(MAKE) $(AM_MAKEFLAGS) am--refresh ) \
	        && { if test -f $@; then exit 0; else break; fi; }; \
	      exit 1;; \
	  esac; \
	done; \
	echo ' cd $(top_srcdir) && $(AUTOMAKE) --foreign examples/Makefile'; \
	$(am__cd) $(top_srcdir) && \
	  $(AUTOMAKE) --foreign examples/Makefile
.PRECIOUS: Makefile
Makefile: $(srcdir)/Makefile.in $(top_builddir)/config.status
	@case '$?' in \
	  *config.status*) \
	    cd $(top_builddir) && $(MAKE) $(AM_MAKEFLAGS) am--refresh;; \
	  *) \
	    echo ' cd $(top_builddir) && $(SHELL) ./config.status $(subdir)/$@ $(am__depfiles_maybe)'; \
	    cd $(top_builddir) && $(SHELL) ./config.status $(subdir)/$@ $(am__depfiles_maybe);; \
	esac;
$(srcdir)/heterotachy/target.mk $(srcdir)/lg4/target.mk $(srcdir)/load-utree/target.mk $(srcdir)/newick-fasta-rooted/target.mk $(srcdir)/newick-fasta-unrooted/target.mk $(srcdir)/newick-phylip-unrooted/target.mk $(srcdir)/newton/target.mk $(srcdir)/parsimony/target.mk $(srcdir)/partial-traversal/target.mk $(srcdir)/protein-list/target.mk $(srcdir)/rooted/target.mk $(srcdir)/rooted-tacg/target.mk $(srcdir)/unrooted/target.mk $(srcdir)/stepwise/target.mk:

$(top_builddir)/config.status: $(top_srcdir)/configure $(CONFIG_STATUS_DEPENDENCIES)
	cd $(top_builddir) && $(MAKE) $(AM_MAKEFLAGS) am--refresh

$(top_srcdir)/configure:  $(am__configure_deps)
	cd $(top_builddir) && $(MAKE) $(AM_MAKEFLAGS) am--refresh
$(ACLOCAL_M4):  $(am__aclocal_m4_deps)
	cd $(top_builddir) && $(MAKE) $(AM_MAKEFLAGS) am--refresh
$(am__aclocal_m4_deps):

mostlyclean-libtool:
	-rm -f *.lo

clean-libtool:
	-rm -rf .libs _libs
install-dist_heterotachyDATA: $(dist_heterotachy_DATA)
	@$(NORMAL_INSTALL)
	@list='$(dist_heterotachy_DATA)'; test -n "$(heterotachydir)" || list=; \
	if test -n "$$list"; then \
	  echo " $(MKDIR_P) '$(DESTDIR)$(heterotachydir)'"; \
	  $(MKDIR_P) "$(DESTDIR)$(heterotachydir)" || exit 1; \
	fi; \
	for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  echo "$$d$$p"; \
	done | $(am__base_list) | \
	while read files; do \
	  echo " $(INSTALL_DATA) $$files '$(DESTDIR)$(heterotachydir)'"; \
	  $(INSTALL_DATA) $$files "$(DESTDIR)$(heterotachydir)" || exit $$?; \
	done

uninstall-dist_heterotachyDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(dist_heterotachy_DATA)'; test -n "$(heterotachydir)" || list=; \
	files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
	dir='$(DESTDIR)$(heterotachydir)'; $(am__uninstall_files_from_dir)
install-dist_lg4DATA: $(dist_lg4_DATA)
	@$(NORMAL_INSTALL)
	@list='$(dist_lg4_DATA)'; test -n "$(lg4dir)" || list=; \
	if test -n "$$list"; then \
	  echo " $(MKDIR_P) '$(DESTDIR)$(lg4dir)'"; \
	  $(MKDIR_P) "$(DESTDIR)$(lg4dir)" || exit 1; \
	fi; \
	for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  echo "$$d$$p"; \
	done | $(am__base_list) | \
	while read files; do \
	  echo " $(INSTALL_DATA) $$files '$(DESTDIR)$(lg4dir)'"; \
	  $(INSTALL_DATA) $$files "$(DESTDIR)$(lg4dir)" || exit $$?; \
	done

uninstall-dist_lg4DATA:
	@$(NORMAL_UNINSTALL)
	@list='$(dist_lg4_DATA)'; test -n "$(lg4dir)" || list=; \
	files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
	dir='$(DESTDIR)$(lg4dir)'; $(am__uninstall_files_from_dir)
install-dist_lg4_dataDATA: $(dist_lg4_data_DATA)
	@$(NORMAL_INSTALL)
	@list='$(dist_lg4_data_DATA)'; test -n "$(lg4_datadir)" || list=; \
	if test -n "$$list"; then \
	  echo " $(MKDIR_P) '$(DESTDIR)$(lg4_datadir)'"; \
	  $(MKDIR_P) "$(DESTDIR)$(lg4_datadir)" || exit 1; \
	fi; \
	for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  echo "$$d$$p"; \
	done | $(am__base_list) | \
	while read files; do \
	  echo " $(INSTALL_DATA) $$files '$(DESTDIR)$(lg4_datadir)'"; \
	  $(INSTALL_DATA) $$files "$(DESTDIR)$(lg4_datadir)" || exit $$?; \
	done

uninstall-dist_lg4_dataDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(dist_lg4_data_DATA)'; test -n "$(lg4_datadir)" || list=; \
	files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
	dir='$(DESTDIR)$(lg4_datadir)'; $(am__uninstall_files_from_dir)
install-dist_load_utreeDATA: $(dist_load_utree_DATA)
	@$(NORMAL_INSTALL)
	@list='$(dist_load_utree_DATA)'; test -n "$(load_utreedir)" || list=; \
	if test -n "$$list"; then \
	  echo " $(MKDIR_P) '$(DESTDIR)$(load_utreedir)'"; \
	  $(MKDIR_P) "$(DESTDIR)$(load_utreedir)" || exit 1; \
	fi; \
	for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  echo "$$d$$p"; \
	done | $(am__base_list) | \
	while read files; do \
	  echo " $(INSTALL_DATA) $$files '$(DESTDIR)$(load_utreedir)'"; \
	  $(INSTALL_DATA) $$files "$(DESTDIR)$(load_utreedir)" || exit $$?; \
	done

uninstall-dist_load_utreeDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(dist_load_utree_DATA)'; test -n "$(load_utreedir)" || list=; \
	files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
	dir='$(DESTDIR)$(load_utreedir)'; $(am__uninstall_files_from_dir)
install-dist_newick_fasta_rootedDATA: $(dist_newick_fasta_rooted_DATA)
	@$(NORMAL_INSTALL)
	@list='$(dist_newick_fasta_rooted_DATA)'; test -n "$(newick_fasta_rooteddir)" || list=; \
	if test -n "$$list"; then \
	  echo " $(MKDIR_P) '$(DESTDIR)$(newick_fasta_rooteddir)'"; \
	  $(MKDIR_P) "$(DESTDIR)$(newick_fasta_rooteddir)" || exit 1; \
	fi; \
	for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  echo "$$d$$p"; \
	done | $(am__base_list) | \
	while read files; do \
	  echo " $(INSTALL_DATA) $$files '$(DESTDIR)$(newick_fasta_rooteddir)'"; \
	  $(INSTALL_DATA) $$files "$(DESTDIR)$(newick_fasta_rooteddir)" || exit $$?; \
	done

uninstall-dist_newick_fasta_rootedDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(dist_newick_fasta_rooted_DATA)'; test -n "$(newick_fasta_rooteddir)" || list=; \
	files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
	dir='$(DESTDIR)$(newick_fasta_rooteddir)'; $(am__uninstall_files_from_dir)
install-dist_newick_fasta_unrootedDATA: $(dist_newick_fasta_unrooted_DATA)
	@$(NORMAL_INSTALL)
	@list='$(dist_newick_fasta_unrooted_DATA)'; test -n "$(newick_fasta_unrooteddir)" || list=; \
	if test -n "$$list"; then \
	  echo " $(MKDIR_P) '$(DESTDIR)$(newick_fasta_unrooteddir)'"; \
	  $(MKDIR_P) "$(DESTDIR)$(newick_fasta_unrooteddir)" || exit 1; \
	fi; \
	for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  echo "$$d$$p"; \
	done | $(am__base_list) | \
	while read files; do \
	  echo " $(INSTALL_DATA) $$files '$(DESTDIR)$(newick_fasta_unrooteddir)'"; \
	  $(INSTALL_DATA) $$files "$(DESTDIR)$(newick_fasta_unrooteddir)" || exit $$?; \
	done

uninstall-dist_newick_fasta_unrootedDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(dist_newick_fasta_unrooted_DATA)'; test -n "$(newick_fasta_unrooteddir)" || list=; \
	files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
	dir='$(DESTDIR)$(newick_fasta_unrooteddir)'; $(am__uninstall_files_from_dir)
install-dist_newick_phylip_unrootedDATA: $(dist_newick_phylip_unrooted_DATA)
	@$(NORMAL_INSTALL)
	@list='$(dist_newick_phylip_unrooted_DATA)'; test -n "$(newick_phylip_unrooteddir)" || list=; \
	if test -n "$$list"; then \
	  echo " $(MKDIR_P) '$(DESTDIR)$(newick_phylip_unrooteddir)'"; \
	  $(MKDIR_P) "$(DESTDIR)$(newick_phylip_unrooteddir)" || exit 1; \
	fi; \
	for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  echo "$$d$$p"; \
	done | $(am__base_list) | \
	while read files; do \
	  echo " $(INSTALL_DATA) $$files '$(DESTDIR)$(newick_phylip_unrooteddir)'"; \
	  $(INSTALL_DATA) $$files "$(DESTDIR)$(newick_phylip_unrooteddir)" || exit $$?; \
	done

uninstall-dist_newick_phylip_unrootedDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(dist_newick_phylip_unrooted_DATA)'; test -n "$(newick_phylip_unrooteddir)" || list=; \
	files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
	dir='$(DESTDIR)$(newick_phylip_unrooteddir)'; $(am__uninstall_files_from_dir)
install-dist_newtonDATA: $(dist_newton_DATA)
	@$(NORMAL_INSTALL)
	@list='$(dist_newton_DATA)'; test -n "$(newtondir)" || list=; \
	if test -n "$$list"; then \
	  echo " $(MKDIR_P) '$(DESTDIR)$(newtondir)'"; \
	  $(MKDIR_P) "$(DESTDIR)$(newtondir)" || exit 1; \
	fi; \
	for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  echo "$$d$$p"; \
	done | $(am__base_list) | \
	while read files; do \
	  echo " $(INSTALL_DATA) $$files '$(DESTDIR)$(newtondir)'"; \
	  $(INSTALL_DATA) $$files "$(DESTDIR)$(newtondir)" || exit $$?; \
	done

uninstall-dist_newtonDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(dist_newton_DATA)'; test -n "$(newtondir)" || list=; \
	files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
	dir='$(DESTDIR)$(newtondir)'; $(am__uninstall_files_from_dir)
install-dist_parsimonyDATA: $(dist_parsimony_DATA)
	@$(NORMAL_INSTALL)
	@list='$(dist_parsimony_DATA)'; test -n "$(parsimonydir)" || list=; \
	if test -n "$$list"; then \
	  echo " $(MKDIR_P) '$(DESTDIR)$(parsimonydir)'"; \
	  $(MKDIR_P) "$(DESTDIR)$(parsimonydir)" || exit 1; \
	fi; \
	for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  echo "$$d$$p"; \
	done | $(am__base_list) | \
	while read files; do \
	  echo " $(INSTALL_DATA) $$files '$(DESTDIR)$(parsimonydir)'"; \
	  $(INSTALL_DATA) $$files "$(DESTDIR)$(parsimonydir)" || exit $$?; \
	done

uninstall-dist_parsimonyDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(dist_parsimony_DATA)'; test -n "$(parsimonydir)" || list=; \
	files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
	dir='$(DESTDIR)$(parsimonydir)'; $(am__uninstall_files_from_dir)
install-dist_partial_traversalDATA: $(dist_partial_traversal_DATA)
	@$(NORMAL_INSTALL)
	@list='$(dist_partial_traversal_DATA)'; test -n "$(partial_traversaldir)" || list=; \
	if test -n "$$list"; then \
	  echo " $(MKDIR_P) '$(DESTDIR)$(partial_traversaldir)'"; \
	  $(MKDIR_P) "$(DESTDIR)$(partial_traversaldir)" || exit 1; \
	fi; \
	for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  echo "$$d$$p"; \
	done | $(am__base_list) | \
	while read files; do \
	  echo " $(INSTALL_DATA) $$files '$(DESTDIR)$(partial_traversaldir)'"; \
	  $(INSTALL_DATA) $$files "$(DESTDIR)$(partial_traversaldir)" || exit $$?; \
	done

uninstall-dist_partial_traversalDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(dist_partial_traversal_DATA)'; test -n "$(partial_traversaldir)" || list=; \
	files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
	dir='$(DESTDIR)$(partial_traversaldir)'; $(am__uninstall_files_from_dir)
install-dist_protein_listDATA: $(dist_protein_list_DATA)
	@$(NORMAL_INSTALL)
	@list='$(dist_protein_list_DATA)'; test -n "$(protein_listdir)" || list=; \
	if test -n "$$list"; then \
	  echo " $(MKDIR_P) '$(DESTDIR)$(protein_listdir)'"; \
	  $(MKDIR_P) "$(DESTDIR)$(protein_listdir)" || exit 1; \
	fi; \
	for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  echo "$$d$$p"; \
	done | $(am__base_list) | \
	while read files; do \
	  echo " $(INSTALL_DATA) $$files '$(DESTDIR)$(protein_listdir)'"; \
	  $(INSTALL_DATA) $$files "$(DESTDIR)$(protein_listdir)" || exit $$?; \
	done

uninstall-dist_protein_listDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(dist_protein_list_DATA)'; test -n "$(protein_listdir)" || list=; \
	files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
	dir='$(DESTDIR)$(protein_listdir)'; $(am__uninstall_files_from_dir)
install-dist_rootedDATA: $(dist_rooted_DATA)
	@$(NORMAL_INSTALL)
	@list='$(dist_rooted_DATA)'; test -n "$(rooteddir)" || list=; \
	if test -n "$$list"; then \
	  echo " $(MKDIR_P) '$(DESTDIR)$(rooteddir)'"; \
	  $(MKDIR_P) "$(DESTDIR)$(rooteddir)" || exit 1; \
	fi; \
	for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  echo "$$d$$p"; \
	done | $(am__base_list) | \
	while read files; do \
	  echo " $(INSTALL_DATA) $$files '$(DESTDIR)$(rooteddir)'"; \
	  $(INSTALL_DATA) $$files "$(DESTDIR)$(rooteddir)" || exit $$?; \
	done

uninstall-dist_rootedDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(dist_rooted_DATA)'; test -n "$(rooteddir)" || list=; \
	files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
	dir='$(DESTDIR)$(rooteddir)'; $(am__uninstall_files_from_dir)
install-dist_rooted_tacgDATA: $(dist_rooted_tacg_DATA)
	@$(NORMAL_INSTALL)
	@list='$(dist_rooted_tacg_DATA)'; test -n "$(rooted_tacgdir)" || list=; \
	if test -n "$$list"; then \
	  echo " $(MKDIR_P) '$(DESTDIR)$(rooted_tacgdir)'"; \
	  $(MKDIR_P) "$(DESTDIR)$(rooted_tacgdir)" || exit 1; \
	fi; \
	for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  echo "$$d$$p"; \
	done | $(am__base_list) | \
	while read files; do \
	  echo " $(INSTALL_DATA) $$files '$(DESTDIR)$(rooted_tacgdir)'"; \
	  $(INSTALL_DATA) $$files "$(DESTDIR)$(rooted_tacgdir)" || exit $$?; \
	done

uninstall-dist_rooted_tacgDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(dist_rooted_tacg_DATA)'; test -n "$(rooted_tacgdir)" || list=; \
	files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
	dir='$(DESTDIR)$(rooted_tacgdir)'; $(am__uninstall_files_from_dir)
install-dist_stepwiseDATA: $(dist_stepwise_DATA)
	@$(NORMAL_INSTALL)
	@list='$(dist_stepwise_DATA)'; test -n "$(stepwisedir)" || list=; \
	if test -n "$$list"; then \
	  echo " $(MKDIR_P) '$(DESTDIR)$(stepwisedir)'"; \
	  $(MKDIR_P) "$(DESTDIR)$(stepwisedir)" || exit 1; \
	fi; \
	for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  echo "$$d$$p"; \
	done | $(am__base_list) | \
	while read files; do \
	  echo " $(INSTALL_DATA) $$files '$(DESTDIR)$(stepwisedir)'"; \
	  $(INSTALL_DATA) $$files "$(DESTDIR)$(stepwisedir)" || exit $$?; \
	done

uninstall-dist_stepwiseDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(dist_stepwise_DATA)'; test -n "$(stepwisedir)" || list=; \
	files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
	dir='$(DESTDIR)$(stepwisedir)'; $(am__uninstall_files_from_dir)
install-dist_unrootedDATA: $(dist_unrooted_DATA)
	@$(NORMAL_INSTALL)
	@list='$(dist_unrooted_DATA)'; test -n "$(unrooteddir)" || list=; \
	if test -n "$$list"; then \
	  echo " $(MKDIR_P) '$(DESTDIR)$(unrooteddir)'"; \
	  $(MKDIR_P) "$(DESTDIR)$(unrooteddir)" || exit 1; \
	fi; \
	for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  echo "$$d$$p"; \
	done | $(am__base_list) | \
	while read files; do \
	  echo " $(INSTALL_DATA) $$files '$(DESTDIR)$(unrooteddir)'"; \
	  $(INSTALL_DATA) $$files "$(DESTDIR)$(unrooteddir)" || exit $$?; \
	done

uninstall-dist_unrootedDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(dist_unrooted_DATA)'; test -n "$(unrooteddir)" || list=; \
	files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
	dir='$(DESTDIR)$(unrooteddir)'; $(am__uninstall_files_from_dir)
tags TAGS:

ctags CTAGS:

cscope cscopelist:


distdir: $(DISTFILES)
	@srcdirstrip=`echo "$(srcdir)" | sed 's/[].[^$$\\*]/\\\\&/g'`; \
	topsrcdirstrip=`echo "$(top_srcdir)" | sed 's/[].[^$$\\*]/\\\\&/g'`; \
	list='$(DISTFILES)'; \
	  dist_files=`for file in $$list; do echo $$file; done | \
	  sed -e "s|^$$srcdirstrip/||;t" \
	      -e "s|^$$topsrcdirstrip/|$(top_builddir)/|;t"`; \
	case $$dist_files in \
	  */*) $(MKDIR_P) `echo "$$dist_files" | \
			   sed '/\//!d;s|^|$(distdir)/|;s,/[^/]*$$,,' | \
			   sort -u` ;; \
	esac; \
	for file in $$dist_files; do \
	  if test -f $$file || test -d $$file; then d=.; else d=$(srcdir); fi; \
	  if test -d $$d/$$file; then \
	    dir=`echo "/$$file" | sed -e 's,/[^/]*$$,,'`; \
	    if test -d "$(distdir)/$$file"; then \
	      find "$(distdir)/$$file" -type d ! -perm -700 -exec chmod u+rwx {} \;; \
	    fi; \
	    if test -d $(srcdir)/$$file && test $$d != $(srcdir); then \
	      cp -fpR $(srcdir)/$$file "$(distdir)$$dir" || exit 1; \
	      find "$(distdir)/$$file" -type d ! -perm -700 -exec chmod u+rwx {} \;; \
	    fi; \
	    cp -fpR $$d/$$file "$(distdir)$$dir" || exit 1; \
	  else \
	    test -f "$(distdir)/$$file" \
	    || cp -p $$d/$$file "$(distdir)/$$file" \
	    || exit 1; \
	  fi; \
	done
check-am: all-am
check: check-am
all-am: Makefile $(DATA)
installdirs:
	for dir in "$(DESTDIR)$(heterotachydir)" "$(DESTDIR)$(lg4dir)" "$(DESTDIR)$(lg4_datadir)" "$(DESTDIR)$(load_utreedir)" "$(DESTDIR)$(newick_fasta_rooteddir)" "$(DESTDIR)$(newick_fasta_unrooteddir)" "$(DESTDIR)$(newick_phylip_unrooteddir)" "$(DESTDIR)$(newtondir)" "$(DESTDIR)$(parsimonydir)" "$(DESTDIR)$(partial_traversaldir)" "$(DESTDIR)$(protein_listdir)" "$(DESTDIR)$(rooteddir)" "$(DESTDIR)$(rooted_tacgdir)" "$(DESTDIR)$(stepwisedir)" "$(DESTDIR)$(unrooteddir)"; do \
	  test -z "$$dir" || $(MKDIR_P) "$$dir"; \
	done
install: install-am
install-exec: install-exec-am
install-data: install-data-am
uninstall: uninstall-am

install-am: all-am
	@$(MAKE) $(AM_MAKEFLAGS) install-exec-am install-data-am

installcheck: installcheck-am
install-strip:
	if test -z '$(STRIP)'; then \
	  $(MAKE) $(AM_MAKEFLAGS) INSTALL_PROGRAM="$(INSTALL_STRIP_PROGRAM)" \
	    install_sh_PROGRAM="$(INSTALL_STRIP_PROGRAM)" INSTALL_STRIP_FLAG=-s \
	      install; \
	else \
	  $(MAKE) $(AM_MAKEFLAGS) INSTALL_PROGRAM="$(INSTALL_STRIP_PROGRAM)" \
	    install_sh_PROGRAM="$(INSTALL_STRIP_PROGRAM)" INSTALL_STRIP_FLAG=-s \
	    "INSTALL_PROGRAM_ENV=STRIPPROG='$(STRIP)'" install; \
	fi
mostlyclean-generic:

clean-generic:

distclean-generic:
	-test -z "$(CONFIG_CLEAN_FILES)" || rm -f $(CONFIG_CLEAN_FILES)
	-test . = "$(srcdir)" || test -z "$(CONFIG_CLEAN_VPATH_FILES)" || rm -f $(CONFIG_CLEAN_VPATH_FILES)

maintainer-clean-generic:
	@echo "This command is intended for maintainers to use"
	@echo "it deletes files that may require special tools to rebuild."
clean: clean-am

clean-am: clean-generic clean-libtool mostlyclean-am

distclean: distclean-am
	-rm -f Makefile
distclean-am: clean-am distclean-generic

dvi: dvi-am

dvi-am:

html: html-am

html-am:

info: info-am

info-am:

install-data-am: install-dist_heterotachyDATA install-dist_lg4DATA \
	install-dist_lg4_dataDATA install-dist_load_utreeDATA \
	install-dist_newick_fasta_rootedDATA \
	install-dist_newick_fasta_unrootedDATA \
	install-dist_newick_phylip_unrootedDATA \
	install-dist_newtonDATA install-dist_parsimonyDATA \
	install-dist_partial_traversalDATA \
	install-dist_protein_listDATA install-dist_rootedDATA \
	install-dist_rooted_tacgDATA install-dist_stepwiseDATA \
	install-dist_unrootedDATA

install-dvi: install-dvi-am

install-dvi-am:

install-exec-am:

install-html: install-html-am

install-html-am:

install-info: install-info-am

install-info-am:

install-man:

install-pdf: install-pdf-am

install-pdf-am:

install-ps: install-ps-am

install-ps-am:

installcheck-am:

maintainer-clean: maintainer-clean-am
	-rm -f Makefile
maintainer-clean-am: distclean-am maintainer-clean-generic

mostlyclean: mostlyclean-am

mostlyclean-am: mostlyclean-generic mostlyclean-libtool

pdf: pdf-am

pdf-am:

ps: ps-am

ps-am:

uninstall-am: uninstall-dist_heterotachyDATA uninstall-dist_lg4DATA \
	uninstall-dist_lg4_dataDATA uninstall-dist_load_utreeDATA \
	uninstall-dist_newick_fasta_rootedDATA \
	uninstall-dist_newick_fasta_unrootedDATA \
	uninstall-dist_newick_phylip_unrootedDATA \
	uninstall-dist_newtonDATA uninstall-dist_parsimonyDATA \
	uninstall-dist_partial_traversalDATA \
	uninstall-dist_protein_listDATA uninstall-dist_rootedDATA \
	uninstall-dist_rooted_tacgDATA uninstall-dist_stepwiseDATA \
	uninstall-dist_unrootedDATA

.MAKE: install-am install-strip

.PHONY: all all-am check check-am clean clean-generic clean-libtool \
	cscopelist-am ctags-am distclean distclean-generic \
	distclean-libtool distdir dvi dvi-am html html-am info info-am \
	install install-am install-data install-data-am \
	install-dist_heterotachyDATA install-dist_lg4DATA \
	install-dist_lg4_dataDATA install-dist_load_utreeDATA \
	install-dist_newick_fasta_rootedDATA \
	install-dist_newick_fasta_unrootedDATA \
	install-dist_newick_phylip_unrootedDATA \
	install-dist_newtonDATA install-dist_parsimonyDATA \
	install-dist_partial_traversalDATA \
	install-dist_protein_listDATA install-dist_rootedDATA \
	install-dist_rooted_tacgDATA install-dist_stepwiseDATA \
	install-dist_unrootedDATA install-dvi install-dvi-am \
	install-exec install-exec-am install-html install-html-am \
	install-info install-info-am install-man install-pdf \
	install-pdf-am install-ps install-ps-am install-strip \
	installcheck installcheck-am installdirs maintainer-clean \
	maintainer-clean-generic mostlyclean mostlyclean-generic \
	mostlyclean-libtool pdf pdf-am ps ps-am tags-am uninstall \
	uninstall-am uninstall-dist_heterotachyDATA \
	uninstall-dist_lg4DATA uninstall-dist_lg4_dataDATA \
	uninstall-dist_load_utreeDATA \
	uninstall-dist_newick_fasta_rootedDATA \
	uninstall-dist_newick_fasta_unrootedDATA \
	uninstall-dist_newick_phylip_unrootedDATA \
	uninstall-dist_newtonDATA uninstall-dist_parsimonyDATA \
	uninstall-dist_partial_traversalDATA \
	uninstall-dist_protein_listDATA uninstall-dist_rootedDATA \
	uninstall-dist_rooted_tacgDATA uninstall-dist_stepwiseDATA \
	uninstall-dist_unrootedDATA


# Tell versions [3.59,3.63) of GNU make to not export all variables.
# Otherwise a system limit (for SysV at least) may be exceeded.
.NOEXPORT:
